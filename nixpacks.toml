[phases.setup]
nixPkgs = ["nodejs_20", "openssl"]

[phases.install]
cmds = ["cd backend && npm install && npx prisma generate"]

[phases.build]
cmds = ["cd frontend && npm install && npm run build"]

[start]
cmd = "cd backend && npx prisma migrate deploy && node src/server.js"
